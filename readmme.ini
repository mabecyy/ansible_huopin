注意事项:
所有主机需要 bash环境下运行 ubuntu 默认是dash 环境 可以使用 `dpkg-reconfigure dash` 命令更改为bash


hosts 变量不同主机相同变量名，不同的变量值
group_vars 定义所有主机共同的变量值

以下为各个role自定义的变量
consul role vars 变量定义
1、consul变量的修改
2、游戏配置文件的修改

cassandra role vars 变量定义
1、master ip定义

redis role vars 变量定义
1、redis ip定义

mysql 变量定义
1、无需更改
